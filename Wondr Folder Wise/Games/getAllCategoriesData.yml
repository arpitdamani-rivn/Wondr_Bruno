info:
  name: getAllCategoriesData
  type: http
  seq: 1

http:
  method: GET
  url: "{{local}}/games/category"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
