info:
  name: notifyUsersTest
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/users/notifyUsers"
  params:
    - name: tournament_id
      value: 675a8d28e8895fca1bd6b7fc
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
