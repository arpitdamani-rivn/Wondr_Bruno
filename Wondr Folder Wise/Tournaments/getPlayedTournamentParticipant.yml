info:
  name: getPlayedTournamentParticipant
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/tournaments/getPlayedTournamentParticipant"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "tournamentId": "694a319694d7ec1876406670"
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
