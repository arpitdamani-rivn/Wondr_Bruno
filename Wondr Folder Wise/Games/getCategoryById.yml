info:
  name: getCategoryById
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/games/category/:id"
  params:
    - name: id
      value: 66d47d2bbcaadfe7dd6bb33a
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
