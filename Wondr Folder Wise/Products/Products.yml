info:
  name: Products
  type: http
  seq: 7

http:
  method: GET
  url: "{{local_dev}}/products"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
