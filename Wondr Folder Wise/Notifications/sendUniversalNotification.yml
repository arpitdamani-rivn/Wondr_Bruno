info:
  name: sendUniversalNotification
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/notifications/sendUniversalNotification"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "title": "fhfhfd",
          "message": "hfhgjgh" 
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
