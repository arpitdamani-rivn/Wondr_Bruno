info:
  name: gameInfo
  type: http
  seq: 2

http:
  method: GET
  url: "{{local}}/games/:gameId"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: gameId
      value: 69088d8cfa25f873dbe0290d
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
