info:
  name: Get Order by Id
  type: http
  seq: 4

http:
  method: GET
  url: "{{local_dev}}/orders/:orderId"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: orderId
      value: 69e8aa7d40febe47359f18be
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
