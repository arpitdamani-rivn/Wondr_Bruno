info:
  name: List Orders
  type: http
  seq: 2

http:
  method: GET
  url: "{{local_dev}}/orders/listOrders"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
