info:
  name: trackCreditTransactions
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/transactions/trackCreditTransactions"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
