info:
  name: List Live leaderboards
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/leaderboards/live"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
