info:
  name: walletScreen
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/wallets/getWalletDetails"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
