info:
  name: verify email otp
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/v1/auth/verifications/email/verify-otp"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "email":"arpitdamani07@gmail.com",
        "otp":2509
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
