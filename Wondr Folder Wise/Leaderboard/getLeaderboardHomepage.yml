info:
  name: getLeaderboardHomepage
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/leaderboards/getLeaderboardHomepage"
  params:
    - name: product_name
      value: ""
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
