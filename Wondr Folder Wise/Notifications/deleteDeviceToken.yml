info:
  name: deleteDeviceToken
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{base_url}}/users/deleteDeviceToken"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "device_id": "123"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
