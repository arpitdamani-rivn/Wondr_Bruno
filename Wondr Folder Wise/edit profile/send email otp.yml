info:
  name: send email otp
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/v1/auth/verifications/email/send-otp"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "email":"arpitdamani07@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
