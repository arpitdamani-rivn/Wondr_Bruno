info:
  name: Homepage Coming Soon and MyZone
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/homes/getHomepageTournaments"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
