info:
  name: aboutIndibid
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/users/aboutIndibid"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
