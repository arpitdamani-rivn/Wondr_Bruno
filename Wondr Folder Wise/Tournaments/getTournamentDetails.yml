info:
  name: getTournamentDetails
  type: http
  seq: 18

http:
  method: GET
  url: "{{base_url}}/tournaments/:tournamentId"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: tournamentId
      value: 69c2317ce9fcbe315b2a4a2f
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
