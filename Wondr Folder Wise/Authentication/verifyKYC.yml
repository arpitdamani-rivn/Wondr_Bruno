info:
  name: verifyKYC
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/users/verifyPan"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "name": "ARKIN AJAY JAIN",
          "pancard": "AFZPJ3800P",
          "dob": "2001-12-12"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
