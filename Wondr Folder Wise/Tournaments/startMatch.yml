info:
  name: startMatch
  type: http
  seq: 4

http:
  method: POST
  url: "{{local_dev}}/tournaments/startMatch"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "participantId": "69dfc0b5b99d8a63303a8443"
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
