info:
  name: resend email otp
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/v1/auth/verifications/email/resend-otp"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "email":"arpitdamani07@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
