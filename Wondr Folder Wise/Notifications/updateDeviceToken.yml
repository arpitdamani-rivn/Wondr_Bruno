info:
  name: updateDeviceToken
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/users/updateDeviceToken"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "device_token": "123",
          "device_id": "123"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
