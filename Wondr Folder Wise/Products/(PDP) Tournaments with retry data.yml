info:
  name: (PDP) Tournaments with retry data
  type: http
  seq: 4

http:
  method: GET
  url: "{{local_dev}}/products/:productId/tournaments"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: productId
      value: 69b114a5c967a4db1d9cd425
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
