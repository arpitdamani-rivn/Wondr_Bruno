info:
  name: results
  type: http
  seq: 13

http:
  method: GET
  url: "{{base_url}}/standings"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
