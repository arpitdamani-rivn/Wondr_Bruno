info:
  name: recalculateWallet
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/wallets/recalculateWallet"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
