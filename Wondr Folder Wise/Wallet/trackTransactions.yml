info:
  name: trackTransactions
  type: http
  seq: 1

http:
  method: GET
  url: "{{local}}/transactions/trackTransactions"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
