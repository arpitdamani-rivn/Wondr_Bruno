info:
  name: SendEmail OTP
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/auth/email/send-otp"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "email":"arpitdamani003@gmail.com"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
