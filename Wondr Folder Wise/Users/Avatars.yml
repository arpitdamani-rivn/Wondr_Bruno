info:
  name: Avatars
  type: http
  seq: 15

http:
  method: GET
  url: http://localhost:3007/storefronts/avatars
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
