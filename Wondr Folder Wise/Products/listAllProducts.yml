info:
  name: listAllProducts
  type: http
  seq: 1

http:
  method: GET
  url: "{{local}}/products/listAllProducts"
  params:
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "10"
      type: query
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
