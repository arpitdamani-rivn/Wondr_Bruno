info:
  name: getPoolRules
  type: http
  seq: 11

http:
  method: GET
  url: "{{base_url}}/config/pool-rules"
  headers:
    - name: User-Agent
      value: insomnia/12.4.0
    - name: Authorization
      value: Bearer {{accessToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
