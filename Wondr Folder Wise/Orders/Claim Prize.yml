info:
  name: Claim Prize
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/orders/claimPrize"
  params:
    - name: tournament_id
      value: 67ef7097c23523361cf74574
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
