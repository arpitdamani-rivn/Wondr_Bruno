info:
  name: getAllLeaderboards
  type: http
  seq: 3

http:
  method: GET
  url: "{{local}}/leaderboards/getAllLeaderboards"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
