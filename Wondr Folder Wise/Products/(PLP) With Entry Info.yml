info:
  name: (PLP) With Entry Info
  type: http
  seq: 3

http:
  method: GET
  url: "{{_['base_url']}}/products/with-entry-info"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
