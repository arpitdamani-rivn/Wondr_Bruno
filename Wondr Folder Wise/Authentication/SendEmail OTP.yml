info:
  name: SendEmail OTP
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/users/sendEmailOTP"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "email":"arkin@noesis.techh"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
