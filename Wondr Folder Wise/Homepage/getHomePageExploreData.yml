info:
  name: getHomePageExploreData
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/homes/getHomePageExploreData"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
