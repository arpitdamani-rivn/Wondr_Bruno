info:
  name: gameRules
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/games/gameRules/:gameId"
  params:
    - name: gameId
      value: 66d47d59bcaadfe7dd6bb361
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
