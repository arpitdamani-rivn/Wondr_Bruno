info:
  name: getActiveTournamentsByGameId/:gameId
  type: http
  seq: 12

http:
  method: GET
  url: "{{base_url}}/tournaments/getActiveTournamentsByGameId/:gameId"
  params:
    - name: gameId
      value: 66d47d59bcaadfe7dd6bb361
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
