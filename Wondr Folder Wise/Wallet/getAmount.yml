info:
  name: getAmount
  type: http
  seq: 4

http:
  method: GET
  url: "{{local}}/wallets/getAmount"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
