info:
  name: getLeaderboard Copy
  type: http
  seq: 5

http:
  method: GET
  url: "{{local_dev}}/leaderboards/generateLeaderboard"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
