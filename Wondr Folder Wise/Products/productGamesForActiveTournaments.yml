info:
  name: productGamesForActiveTournaments
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/products/productGamesForActiveTournaments/:productId"
  params:
    - name: productId
      value: 673b0d6d14cf2cb5272e5dd9
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
