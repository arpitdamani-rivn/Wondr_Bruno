info:
  name: Claim Order
  type: http
  seq: 7

http:
  method: POST
  url: "{{local_dev}}/orders/:orderId/claim"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: orderId
      value: 69e8aa7d40febe47359f18be
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
