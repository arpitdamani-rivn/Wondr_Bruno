info:
  name: productListing
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/products/productListing"
  params:
    - name: page
      value: "1"
      type: query
      disabled: true
    - name: limit
      value: ""
      type: query
      disabled: true
    - name: query
      value: ""
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
