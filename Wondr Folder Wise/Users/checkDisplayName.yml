info:
  name: checkDisplayName
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/users/checkDisplayName"
  params:
    - name: displayName
      value: ""
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
